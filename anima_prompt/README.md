# ANIMA Prompt Compatibility Package

The implementation is canonical under `easyuse_anima.prompt.anima`.
This root package keeps the supported historical import paths as explicit
object-identity shims during the compatibility period.
