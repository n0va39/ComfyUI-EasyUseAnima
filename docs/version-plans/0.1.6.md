# 0.1.6 버전 계획

## 상태

- [ ] 계획 검토 완료
- [ ] 구현 시작
- [ ] 기능 구현 완료
- [ ] 로컬 검증 완료
- [ ] ComfyUI 인스턴스 반영 완료
- [ ] 릴리즈 준비 완료

## 목표

0.1.6에서는 두 가지 작업을 진행한다.

- 자동완성 동작 범위를 사용자가 선택할 수 있게 한다.
- Impact Pack의 디테일러 구조와 참고용 워크플로우의 노드 구성을 보고 ANIMA용 디테일러 노드 설계를 시작한다.

ANIMA 디테일러는 범위가 크므로 한 번에 완성하지 않는다. `docs/workflows/Anima_AiO_v5.5.1.json`은 참고용 워크플로우이며, 아래 노드 번호는 그 파일 안에서 구현 참고 대상을 찾기 위한 기준이다. 먼저 참고 워크플로우에서 실제로 쓰는 `MaskToSEGS`와 `DetailerForEach`의 입출력과 기본값을 확인하고, 최소 기능으로 검증 가능한 형태를 만든다.

## 전체 체크리스트

- [ ] `pyproject.toml` 버전을 `0.1.6`으로 유지한다.
- [ ] 자동완성 설정 UI를 추가한다.
- [ ] 자동완성 적용 범위 로직을 추가한다.
- [ ] 자동완성 설정 저장/불러오기를 확인한다.
- [ ] ANIMA 디테일러 참고 대상 코드를 확인한다.
- [ ] 참고용 워크플로우 `docs/workflows/Anima_AiO_v5.5.1.json`에서 `1530` 서브그래프 구조를 확인한다.
- [ ] 참고용 워크플로우의 `1518` `MaskToSEGS` 설정값을 확인한다.
- [ ] 참고용 워크플로우의 `1527` `DetailerForEach` 설정값을 확인한다.
- [ ] `1518`의 사용 패턴을 바탕으로 `Anima Mask to SEGS` 초안을 만든다.
- [ ] `1527`의 사용 패턴을 바탕으로 `Anima Detailer` 초안을 만든다.
- [ ] 16채널 VAE와 크기 정렬 문제를 처리할 정책을 정한다.
- [ ] 최소 테스트를 추가한다.
- [ ] ComfyUI 실제 인스턴스에서 워크플로우 로딩과 실행을 확인한다.

## 1. 자동완성 설정

### 목표

EasyUse Anima 자동완성이 어디에서 동작할지 설정할 수 있게 한다.

### 설정 모드

- [ ] `off`
  - EasyUse Anima 자동완성을 전부 끈다.
  - 입력 이벤트 핸들러를 붙이지 않는다.
  - 자동완성 팝업을 열지 않는다.
  - `/easyuse_anima/autocomplete` API를 호출하지 않는다.

- [ ] `easyuse_nodes`
  - EasyUse Anima 전용 프롬프트 노드에서만 자동완성을 사용한다.
  - 대상:
    - Anima Prompt Studio
    - Anima Prompt Studio Advanced
    - 이후 추가되는 EasyUse Anima 프롬프트 편집 노드
  - 일반 ComfyUI 문자열 입력 위젯에는 붙이지 않는다.

- [ ] `compatible_global`
  - 현재 방식이다.
  - 지원 가능한 일반 텍스트 위젯에도 자동완성을 붙인다.
  - LoRA Manager, LoRA Stacker 등 자체 자동완성이나 LoRA 전용 입력을 가진 노드는 계속 제외한다.

### 기본값

- [ ] 기본값은 `compatible_global`로 둔다.
- [ ] 기존 사용자 동작이 갑자기 바뀌지 않게 한다.

### 구현 방법

- [ ] 설정 키를 추가한다.
  - 후보: `autocomplete.mode`
- [ ] `web/js/easyuse_anima_settings.js`에 설정 UI를 추가한다.
  - 기존 자동완성 CSV 선택과 결과 개수 설정 근처에 배치한다.
- [ ] `web/js/easyuse_anima_autocomplete.js`에 모드 판정 함수를 추가한다.
  - 후보 함수:
    - `autocompleteMode()`
    - `isEasyUseAnimaAutocompleteTarget(widgetOrElement, node)`
    - `shouldAttachEasyUseAutocomplete(widgetOrElement, node)`
- [ ] 자동완성 핸들러를 붙이기 전에 설정을 확인한다.
- [ ] 팝업을 열기 전에 설정을 확인한다.
- [ ] API 요청 전에 설정을 확인한다.
- [ ] `off`일 때 이미 열린 팝업이 있으면 닫는다.
- [ ] 설정 변경 이벤트를 받아 브라우저 새로고침 없이 가능한 범위에서 반영한다.
- [ ] `compatible_global`에서는 기존 제외 규칙을 유지한다.

### 검증 기준

- [ ] `off`에서 자동완성 팝업이 뜨지 않는다.
- [ ] `off`에서 `/easyuse_anima/autocomplete` 요청이 발생하지 않는다.
- [ ] `easyuse_nodes`에서 EasyUse Anima 프롬프트 노드에서는 자동완성이 동작한다.
- [ ] `easyuse_nodes`에서 일반 문자열 입력 위젯에는 자동완성이 붙지 않는다.
- [ ] `compatible_global`에서 기존 방식처럼 동작한다.
- [ ] LoRA Manager / Lora Stacker 계열 입력창에는 EasyUse Anima 자동완성이 뜨지 않는다.
- [ ] `node --check web/js/easyuse_anima_autocomplete.js` 통과
- [ ] `node --check web/js/easyuse_anima_settings.js` 통과

## 2. ANIMA 디테일러 설계

### 참고 대상

Impact Pack 코드를 참고하되, EasyUse Anima가 Impact Pack 미설치 상태에서 import 실패하지 않게 한다.

참고 파일:

- `D:/ComfyUI/ComfyUI_main/instances/ComfyUI_v0.25.1/custom_nodes/comfyui-impact-pack/modules/impact/impact_pack.py`
  - `DetailerForEach`
  - `DetailerForEachPipe`
- `D:/ComfyUI/ComfyUI_main/instances/ComfyUI_v0.25.1/custom_nodes/comfyui-impact-pack/modules/impact/segs_nodes.py`
  - `MaskToSEGS`
- `D:/ComfyUI/ComfyUI_main/instances/ComfyUI_v0.25.1/custom_nodes/comfyui-impact-pack/__init__.py`
  - Impact Pack 노드 등록명과 표시명

참고용 워크플로우:

- `docs/workflows/Anima_AiO_v5.5.1.json`
- 서브그래프: `1530`
- 디테일러 노드: `1527`
- MASK to SEGS 노드: `1518`

### 워크플로우 기준값

아래 값은 참고용 워크플로우에서 가져온 기준값이다. 이 워크플로우 자체를 배포 대상이나 구현 대상으로 보지 않는다.

`1530` 서브그래프는 다음 흐름을 가진다.

- SAM/마스크 검출
- `MaskToSEGS` 변환
- `DetailerForEach` 기반 얼굴 디테일링
- guide size, max size, denoise, feather, noise mask, force inpaint, wildcard 제어
- 일부 detailer hook 관련 설정

`1527` 디테일러 기준값:

- [ ] 타입: `DetailerForEach`
- [ ] 제목: `얼굴 디테일러 (SEGS)`
- [ ] `guide_size`: `512`
- [ ] `guide_size_for`: `true`
- [ ] `max_size`: `1024`
- [ ] `steps`: `20`
- [ ] `cfg`: `8`
- [ ] `sampler_name`: `euler`
- [ ] `scheduler`: `sgm_uniform`
- [ ] `denoise`: `0.33`
- [ ] `feather`: `5`
- [ ] `noise_mask`: `true`
- [ ] `force_inpaint`: `true`
- [ ] `cycle`: `1`
- [ ] `inpaint_model`: `false`
- [ ] `noise_mask_feather`: `10`
- [ ] `tiled_encode`: `false`
- [ ] `tiled_decode`: `false`

`1518` MASK to SEGS 기준값:

- [ ] 타입: `MaskToSEGS`
- [ ] `combined`: `true`
- [ ] `crop_factor`: `4`
- [ ] `bbox_fill`: `false`
- [ ] `drop_size`: `100`
- [ ] `contour_fill`: `true`

## 3. 추가할 노드 초안

### 3.1 Anima Mask to SEGS

목표:

- [ ] ANIMA 디테일러 워크플로우용 MASK to SEGS 편의 노드를 만든다.
- [ ] 일반 사용자가 Impact Pack의 세부 옵션을 많이 만지지 않아도 되게 한다.
- [ ] 참고용 워크플로우의 `1518` 노드 설정값을 기반으로 기본값을 잡는다.

초기 입력:

- [ ] `mask: MASK`
- [ ] `combined: BOOLEAN`, 기본값 `true`
- [ ] `crop_factor: FLOAT`, 기본값 `4.0`
- [ ] `bbox_fill: BOOLEAN`, 기본값 `false`
- [ ] `drop_size: INT`, 기본값 `100`
- [ ] `contour_fill: BOOLEAN`, 기본값 `true`

초기 출력:

- [ ] `SEGS`

구현 방향:

- [ ] Impact Pack 없이 자체 SEGS 호환 구조를 만들 수 있는지 먼저 확인한다.
- [ ] 자체 구현이 작고 안정적이면 EasyUse Anima 내부 구현으로 간다.
- [ ] SEGS 구조 생성에 Impact Pack 내부 클래스/헬퍼가 꼭 필요하면 선택적 런타임 의존성으로 처리한다.
- [ ] Impact Pack이 없을 때 EasyUse Anima 전체 import가 실패하지 않게 한다.
- [ ] Impact Pack이 필요한 노드는 실행 시점에 명확한 오류를 낸다.

오류 메시지 기준:

- [ ] 어떤 노드가 Impact Pack을 필요로 하는지 표시한다.
- [ ] 필요한 패키지 이름을 표시한다.
- [ ] 설치가 필요한 경우 사용자가 이해할 수 있는 문장으로 안내한다.

### 3.2 Anima Detailer

목표:

- [ ] Impact Pack `DetailerForEach`와 비슷한 데이터 흐름을 유지한다.
- [ ] ANIMA용 기본값과 안전장치를 적용한다.
- [ ] 참고용 워크플로우의 `1527` 노드 설정값을 기반으로 기본값을 잡는다.
- [ ] 너무 많은 Impact Pack 옵션을 그대로 노출하지 않고, ANIMA에 필요한 항목부터 노출한다.

초기 입력:

- [ ] `image: IMAGE`
- [ ] `segs: SEGS`
- [ ] `model: MODEL`
- [ ] `clip: CLIP`
- [ ] `vae: VAE`
- [ ] `positive: CONDITIONING`
- [ ] `negative: CONDITIONING`

초기 옵션:

- [ ] `guide_size`, 기본값 `512`
- [ ] `guide_size_for`, 기본값 `bbox`
- [ ] `max_size`, 기본값 `1024`
- [ ] `seed`
- [ ] `steps`, 기본값 `20`
- [ ] `cfg`, 기본값 `8`
- [ ] `sampler_name`, 기본값 후보 `euler`
- [ ] `scheduler`, 기본값 후보 `sgm_uniform`
- [ ] `denoise`, 기본값 `0.33`
- [ ] `feather`, 기본값 `5`
- [ ] `noise_mask`, 기본값 `true`
- [ ] `force_inpaint`, 기본값 `true`
- [ ] `wildcard`, 기본값 빈 문자열
- [ ] `cycle`, 기본값 `1`
- [ ] `noise_mask_feather`, 기본값 `10`
- [ ] `tiled_encode`
- [ ] `tiled_decode`

초기 출력:

- [ ] `IMAGE`

구현 방향:

- [ ] 처음에는 `DetailerForEach`의 핵심 흐름을 기준으로 한다.
- [ ] batch image 제한 등 Impact Pack의 안정성 조건을 확인한다.
- [ ] wildcard 처리는 첫 구현에서 최소 지원으로 둔다.
- [ ] detailer hook은 첫 구현에서 제외하거나 optional로 둔다.
- [ ] Impact Pack 내부 코드를 대량 복사하지 않는다.

## 4. 16채널 VAE와 크기 보정

문제:

- ANIMA 워크플로우에서 16채널 VAE 또는 특수 VAE를 쓰면 crop/detail 크기 불일치가 발생할 수 있다.
- 일반 4채널 SD VAE 기준으로만 크기를 잡으면 encode/decode 또는 mask 합성 단계에서 문제가 날 수 있다.

원칙:

- [ ] 모든 crop/detail 크기는 안전한 배수로 정렬한다.
- [ ] 우선 32픽셀 배수 정렬을 기본 후보로 둔다.
- [ ] mask 크기와 crop 크기가 서로 어긋나지 않게 한다.
- [ ] guide size와 max size 적용 후 최종 크기를 다시 확인한다.
- [ ] tiled encode/decode가 필요한 경우 숨기지 않고 옵션으로 드러낸다.
- [ ] 크기를 자동 조정했다면 로그나 report로 알 수 있게 한다.

구현 체크리스트:

- [ ] Impact Pack에서 crop region, guide size, max size를 계산하는 위치를 확인한다.
- [ ] ANIMA용 크기 정렬 helper를 만든다.
- [ ] helper 입력을 정한다.
  - crop width
  - crop height
  - guide size
  - max size
  - alignment
- [ ] helper 출력을 정한다.
  - safe width
  - safe height
  - scale decision
  - adjusted 여부
- [ ] guide size/max size를 직접 바꿀지, 최종 crop 크기만 보정할지 결정한다.
- [ ] ComfyUI 런타임 없이 테스트 가능한 단위 테스트를 추가한다.

## 5. 의존성 정책

기본 방향:

- [ ] EasyUse Anima는 Impact Pack이 없어도 import되어야 한다.
- [ ] Impact Pack이 필요한 노드는 실행 시점에만 실패해야 한다.
- [ ] 실패 메시지는 명확해야 한다.
- [ ] `SEGS` 입출력 호환성이 필요한 노드는 문서에 명시한다.

결정이 필요한 부분:

- [ ] SEGS 구조를 EasyUse Anima 내부에서 자체 구현할지 결정한다.
- [ ] Impact Pack을 선택적 런타임 의존성으로 둘지 결정한다.
- [ ] ComfyUI Manager/Registry 배포에서 Impact Pack 의존성을 어떻게 안내할지 결정한다.

판단 기준:

- [ ] 자체 구현이 작고 안정적이면 자체 구현한다.
- [ ] Impact Pack 내부 구조 복제가 커지면 선택적 의존성으로 둔다.
- [ ] EasyUse Anima의 기존 노드가 Impact Pack 미설치 때문에 망가지면 안 된다.

## 6. 검증 계획

정적 검증:

- [ ] `python -m unittest discover -s tests`
- [ ] `python -m compileall -q .`
- [ ] `git diff --check`
- [ ] `powershell -ExecutionPolicy Bypass -File tools/check_custom_node.ps1 -Project ComfyUI-EasyUseAnima`

프론트엔드 검증:

- [ ] `node --check web/js/easyuse_anima_autocomplete.js`
- [ ] `node --check web/js/easyuse_anima_settings.js`

ComfyUI 런타임 검증:

- [ ] 참고용 워크플로우 `docs/workflows/Anima_AiO_v5.5.1.json`에서 `1530` 서브그래프 위치를 확인한다.
- [ ] 참고용 워크플로우에서 `1518`과 `1527`의 연결 관계를 확인한다.
- [ ] 새 노드를 별도 테스트 워크플로우에 배치해 `1518`/`1527` 참고값과 같은 입출력 흐름을 만들 수 있는지 확인한다.
- [ ] 기존 참고용 워크플로우 파일을 구현 결과물로 직접 수정하지 않는다.
- [ ] 16채널 VAE 사용 시 크기 오류가 없는지 확인
- [ ] 기본값으로 예상치 못한 캐시 invalidation이 발생하지 않는지 확인
- [ ] Impact Pack 미설치 상태에서 EasyUse Anima 기존 노드가 import 가능한지 확인

## 7. 첫 구현에서 제외할 것

- [ ] Impact Pack의 모든 detailer mode 대체
- [ ] detector 전체 구현
- [ ] SAM 통합 구현
- [ ] Animatediff/video SEGS 지원
- [ ] Impact Pack UI를 그대로 복제하는 작업
- [ ] detailer hook 전체 구현

## 8. 완료 기준

0.1.6을 완료로 볼 조건:

- [ ] 자동완성 설정 3단계가 동작한다.
- [ ] 설정값이 저장되고 ComfyUI 재시작 후 유지된다.
- [ ] `off` 모드에서 자동완성 API 호출이 발생하지 않는다.
- [ ] `easyuse_nodes` 모드에서 EasyUse Anima 노드에만 자동완성이 적용된다.
- [ ] `compatible_global` 모드에서 기존 동작이 유지된다.
- [ ] ANIMA 디테일러 구현 방향이 확정된다.
- [ ] 최소 1개 이상의 ANIMA 디테일러 관련 노드 초안이 구현된다.
- [ ] 참고용 워크플로우의 `1518`/`1527` 기준값과 새 노드의 동작 비교 결과를 기록한다.
- [ ] 검증 명령이 통과한다.
