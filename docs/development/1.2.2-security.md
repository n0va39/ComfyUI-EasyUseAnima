# 1.2.2 NAIA security work packet

- Owner: #677; Registry policy-v0.3/v0.4 SSRF findings.
- Base: `4a84864099c820ab5d847508d77f10f614c459c5` (`origin/dev`).
- Goal: ordinary settings, Comfy settings overlays and workflows cannot expand
  the NAIA network destinations approved by the process operator.
- Existing companion patch: #785 is already included in this base and v1.2.1;
  retain its per-execution image workflow/metadata corrections in 1.2.2.
- Production scope: NAIA transport and endpoint policy, bootstrap/config and
  existing Comfy runtime wiring; NAIA settings help text only.
- Test/docs scope: direct NAIA/security/runtime tests, required generated Python
  analysis fixtures, operator migration instructions and scanner contract.
- Preserve node IDs, workflow/settings keys, local NAIA on port 7243, explicitly
  approved LAN endpoints, fixed POST path, redirect rejection and metadata fixes.
- Authority: load `EASYUSE_ANIMA_NAIA_ENDPOINTS` once in bootstrap into immutable
  runtime configuration. Never derive permissions from web-writable settings or
  files under ComfyUI's HTTP-writable user data directory.
- Allowlist entries identify an exact host, port and numeric connection address.
  Named hosts need an explicit address so no DNS lookup/rebinding occurs during
  requests. Local defaults use numeric loopback; other local ports need approval.
- Invalid operator configuration disables NAIA requests, with a fixed warning.
  Do not expose configuration values, response bodies or transport errors.
- Size review: bootstrap's module exception grows from 450 to 464 lines solely
  for startup policy loading. Keep this next to immutable RuntimeConfig creation
  and retain the existing E-09 lifecycle owner; pure parsing stays under NAIA.
- Transport: connect only to the approved numeric address; preserve a validated
  Host header; use an isolated Requests session without ambient proxies/netrc.
- Focused: settings API/overlay cannot bypass policy; approved local/LAN success;
  no denied DNS/connect attempt; 3xx rejection; no ambient proxy/auth; startup
  snapshot and immutable configuration; existing metadata readback tests.
- Gates: final full runner, package closure, isolated ComfyUI HTTP/queue smoke,
  and review of the complete security boundary. Frontend changes are text only.
- Stop: a new finding outside this boundary or an overlapping source change.
  Release metadata/artifact preparation follows in a separate commit. Registry
  activation remains an external read-back condition for #677.
