# 0.1.7 Development Plan

This file tracks changes intended for the next package version. It is not a
release announcement.

## Scope

- Keep `pyproject.toml` on the next Registry package version so local work does
  not reuse an already-published package version.
- Do not trigger Comfy Registry publishing, create tags, or publish GitHub
  releases from this plan.

## Planned Changes

- Add LoRA Preset path repair for shared workflow/profile data when local LoRA
  folders differ and a unique filename match exists.
- Add row-level and profile-level `FIX` actions in the LoRA Preset UI.
- Add backend route support for LoRA profile path repair.
- Add regression tests for unique filename repair, ambiguous filename handling,
  unresolved missing paths, and existing-path fast paths.
- Add release-suffixed workflow templates for Registry distribution.
- Document release workflow filename and live-sync rules.
- Document AiO workflow HTML documentation storage under `docs/Anima AiO/`.
- Add per-section `MarkdownNote` guide nodes to the Korean AiO release workflow.
- Clear session-local preview state from the Korean AiO release workflow before distribution.
- Add EN/KO `Anima_AiO_v6.0` Registry workflow templates under
  `docs/example_workflows/`.
- Add EN/KO core feature-test Registry workflow templates under
  `docs/example_workflows/`.
- Align workflow `extra.easyuse_anima_workflow.package_version` metadata with
  package version `0.1.7`.
- Exclude development-only plans and reference workflow copies from the Comfy
  Registry archive through `.comfyignore`.
- Add prompt comment support using line-start `#` comments.
- Add configurable Prompt Studio comment highlighting, including an italic
  style toggle.
- Add autocomplete behavior settings for commit key handling, separator
  insertion, and natural-language sentence handling.
- Improve autocomplete insertion so existing token separators are normalized
  without overwriting separated tokens.

## Release Note Draft

### Added

- Prompt comments can now be written as line-start `#` comments. Comment lines
  are classified separately from prompt tags and can be highlighted in Prompt
  Studio.
- Prompt Studio comment display can be configured, including whether comment
  lines are italicized.
- Autocomplete can now be configured to commit with `Enter` and `Tab`, or with
  `Tab` only. `Shift+Enter` remains a normal line break.
- Autocomplete can optionally append a trailing `, ` after a committed
  suggestion.
- Autocomplete adds options for natural-language handling:
  `Do not comma-correct after period` and `Detect natural text by periods`.
- Registry workflow templates are included for Anima AiO v6.0 and core
  EasyUse Anima feature testing.

### Improved

- Release workflow metadata is aligned with package version `0.1.7`.
- Registry archives exclude development-only planning files and reference
  workflow copies while keeping public workflow templates under
  `docs/example_workflows/`.
- Autocomplete now preserves the selected suggestion while the input text and
  caret position have not changed, so arrow-key navigation no longer resets to
  the first suggestion.
- Autocomplete insertion uses editable range replacement so committed
  suggestions can be undone with the text editor's normal undo shortcut.
- Autocomplete preserves natural-language sentence text before a period and
  completes only the active tag fragment when sentence detection is enabled.
- Existing separators are reused and normalized. For example, completing after
  `wisteria,dd` with separator append enabled moves the caret to
  `wisteria, |dd` and normalizes the text to `wisteria, dd`.

### Fixed

- Fixed comment parsing edge cases introduced by slash-style comment handling;
  prompt comments now use the safer line-start `#` convention.
- Fixed autocomplete cache/refresh behavior that could re-render unchanged
  suggestions and reset the active item.
- Fixed separator handling so autocomplete does not duplicate an existing
  comma or overwrite text that is already separated by a comma, newline, or
  natural-language sentence boundary.
- Fixed stale link ids in the core feature-test workflow templates so workflow
  link integrity checks pass.

## Validation Targets

- `node --check web/js/easyuse_anima_lora_preset.js`
- `node --check web/js/easyuse_anima_autocomplete.js`
- `node --check web/js/easyuse_anima_prompt_studio.js`
- `node --check web/js/easyuse_anima_settings.js`
- `python -m unittest discover -s tests`
- `git diff --check`
- Workflow JSON parse and top-level link integrity checks
- Registry workflow hygiene checks for stale links, stale package versions,
  local preview URLs, clipspace state, and local temp paths
