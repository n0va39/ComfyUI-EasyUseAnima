# 0.2.5 Development Notes

## Scope

0.2.5 is the next planned patch version after the tagged 0.2.4 hotfix.
The current scope starts with Prompt Studio text rendering cleanup and keeps
the work compatible with the existing Prompt Studio, Advanced Prompt Studio,
and Extend node surfaces.

## Current Changes

- Package metadata is set to `0.2.5` as the next-version marker.
- Prompt Studio text areas use the system UI font and root font size by default
  instead of a fixed 12px monospace font in the Advanced editor.
- Prompt Studio highlight overlays re-copy input text metrics after settings
  changes, so the input text and overlay stay aligned.
- A manual Prompt Studio font correction setting is available for display
  mismatch cases only:
  - `prompt_studio.font_override`
  - `prompt_studio.font_family`
  - `prompt_studio.font_size`
- The manual font family and size values are registered in backend defaults,
  Comfy settings key mapping, frontend settings UI, and regression tests.
- AiO Generator now renders Comfy denoising latent previews inside its own image
  preview panel when the frontend receives metadata-backed preview events.

## Planned Follow-Up

- Confirm the default system-font path visually in a live ComfyUI instance.
- Confirm AiO denoising preview rendering visually in a live ComfyUI instance.
- If the user-facing settings text needs screenshots or guide coverage, update
  node documentation after the runtime behavior settles.
- Before release finalization, add the accepted 0.2.5 changes to `RELEASE.md`
  and run the full release validation checklist.

## Validation Checklist

- `node --check web\js\easyuse_anima_prompt_studio.js`
- `node --check web\js\easyuse_anima_settings.js`
- `node --check web\js\easyuse_anima_aio.js`
- `.venv\Scripts\python.exe -m unittest tests.test_prompt_corrector`
- `.venv\Scripts\python.exe -m compileall -q settings.py tests\test_prompt_corrector.py`
- `git diff --check`
- pyproject TOML metadata sanity check

## Release Notes

- Pending. Convert this plan into a `RELEASE.md` 0.2.5 section only when
  release finalization is explicitly requested.
