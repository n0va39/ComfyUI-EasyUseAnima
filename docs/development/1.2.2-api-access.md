# 1.2.2 API access work packet

- Owner: #790, alongside #677 and PR #789.
- Base: `633b0a2de27f370cfbf42b97a3ffbe30799d0457`; dev base
  `4a84864099c820ab5d847508d77f10f614c459c5`.
- Goal: enforce the supported local deployment boundary before all EasyUse
  route handlers and authenticate remote operations with immutable startup data.
- Allowed production files: API request/response and composition owners,
  RuntimeConfig and bootstrap. Tests, generated analysis and access/setup docs
  follow those exact owners. No frontend authentication implementation.
- Preserve route order, success/error correlation contracts, profile revisions,
  node IDs, workflows, metadata, NAIA policy and the single lifecycle owner.
- Default local policy: actual socket peer plus a literal loopback Host, no
  forwarded assertions. Configured token: HTTP Basic required for every peer.
  Never describe local transport checks as principal authentication. Reverse
  proxy/tunnel deployments must enable the token irrespective of bind address.
- Secret digest is excluded from RuntimeConfig repr; no public token endpoint,
  secret file under HTTP-writable user data, web setting, URL or logging sink.
- Focused: all registered routes deny nonlocal calls before work, local/header
  spoofing, malformed/wrong/missing Basic credentials, authenticated success,
  CSRF after authentication, immutable startup snapshot, unavailable runtime.
- Validation: focused existing request/profile behavior, final full, official
  package and isolated ComfyUI 8188 HTTP authentication/profile operations.
  NAIA remains at the user-provided LAN service on 7243. Reuse valid image
  metadata and canvas evidence where these payloads do not change.
- Bootstrap exception grows by four cohesive configuration lines (464 to 468);
  moving these would create a second startup owner without reducing complexity.
- Completion: validated PR merged to dev and local security ledger updated.
  Registry approval and absent historical finding details remain external facts.
- Stop only for an actual new security flaw outside this boundary or conflicting
  worktree ownership; do not expand into a replacement ComfyUI auth service.
